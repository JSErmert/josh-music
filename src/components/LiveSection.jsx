// LiveSection — hidden until there are real show dates.
// (The inherited section listed fabricated venues; we render nothing rather than
// show invented engagements. Restore a real list here when dates exist.)
export default function LiveSection() {
  return null
}
